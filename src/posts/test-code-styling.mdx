---
title: "Test Code Block Styling"
date: "2025-01-17"
tags: ["test", "code", "styling"]
description: "Testing the new professional code block styling implementation"
slug: "test-code-styling"
---

# Test Code Block Styling

This is a test post to verify our new professional code block styling works correctly.

## JavaScript Example

```javascript
// Create a user for the module and its schema
const createUser = async (userData) => {
  try {
    const user = new User(userData);
    await user.save();
    return { success: true, user };
  } catch (error) {
    console.error('Error creating user:', error);
    return { success: false, error: error.message };
  }
};

// Usage example
const newUser = await createUser({
  name: 'John Doe',
  email: 'john@example.com',
  role: 'admin'
});
```

## SQL Example

```sql
-- create a user for the module and its schema
CREATE ROLE orders_role LOGIN PASSWORD 'orders_secret';
CREATE SCHEMA orders AUTHORIZATION orders_role;

-- Grant necessary permissions
GRANT USAGE ON SCHEMA orders TO orders_role;
GRANT ALL PRIVILEGES ON ALL TABLES IN SCHEMA orders TO orders_role;
GRANT ALL PRIVILEGES ON ALL SEQUENCES IN SCHEMA orders TO orders_role;

-- Create orders table
CREATE TABLE orders.order_items (
    id SERIAL PRIMARY KEY,
    order_id INTEGER NOT NULL,
    product_name VARCHAR(255) NOT NULL,
    quantity INTEGER NOT NULL,
    price DECIMAL(10, 2) NOT NULL,
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);
```

## C# Example

```csharp
public class OrderService
{
    private readonly IOrderRepository _orderRepository;
    private readonly ILogger<OrderService> _logger;

    public OrderService(IOrderRepository orderRepository, ILogger<OrderService> logger)
    {
        _orderRepository = orderRepository;
        _logger = logger;
    }

    public async Task<Result<Order>> CreateOrderAsync(CreateOrderRequest request)
    {
        try
        {
            var order = new Order
            {
                CustomerId = request.CustomerId,
                OrderDate = DateTime.UtcNow,
                Status = OrderStatus.Pending
            };

            foreach (var item in request.Items)
            {
                order.AddItem(item.ProductId, item.Quantity, item.Price);
            }

            await _orderRepository.AddAsync(order);
            await _orderRepository.SaveChangesAsync();

            _logger.LogInformation("Order {OrderId} created successfully", order.Id);
            return Result<Order>.Success(order);
        }
        catch (Exception ex)
        {
            _logger.LogError(ex, "Failed to create order for customer {CustomerId}", request.CustomerId);
            return Result<Order>.Failure("Failed to create order");
        }
    }
}
```

## Python Example

```python
import asyncio
from typing import List, Optional
from dataclasses import dataclass

@dataclass
class Order:
    id: Optional[int] = None
    customer_id: int = None
    items: List[dict] = None
    total: float = 0.0
    status: str = "pending"

class OrderService:
    def __init__(self, db_connection):
        self.db = db_connection

    async def create_order(self, customer_id: int, items: List[dict]) -> Order:
        """Create a new order with the given items."""
        try:
            # Calculate total
            total = sum(item['price'] * item['quantity'] for item in items)
            
            # Create order
            order = Order(
                customer_id=customer_id,
                items=items,
                total=total
            )
            
            # Save to database
            order_id = await self.db.save_order(order)
            order.id = order_id
            
            return order
        except Exception as e:
            print(f"Error creating order: {e}")
            raise
```

## JSON Example

```json
{
  "order": {
    "id": 12345,
    "customerId": 67890,
    "status": "pending",
    "items": [
      {
        "productId": 1,
        "name": "MacBook Pro",
        "quantity": 1,
        "price": 2499.99
      },
      {
        "productId": 2,
        "name": "USB-C Hub",
        "quantity": 2,
        "price": 79.99
      }
    ],
    "total": 2659.97,
    "createdAt": "2025-01-17T10:30:00Z"
  }
}
```

## Bash Example

```bash
#!/bin/bash

# Create database and user
echo "Creating database and user..."
psql -U postgres << EOF
CREATE DATABASE orders_db;
CREATE USER orders_user WITH PASSWORD 'secure_password';
GRANT ALL PRIVILEGES ON DATABASE orders_db TO orders_user;
\q
EOF

# Run migrations
echo "Running database migrations..."
npm run migrate

# Start the application
echo "Starting application..."
npm start
```

These examples should demonstrate the professional code styling with:
- ✅ Syntax highlighting for different languages
- ✅ Copy buttons for each code block
- ✅ Language labels
- ✅ Line numbers
- ✅ Professional color scheme
- ✅ Responsive design